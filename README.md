# Alura Git e Github exercicios
revisando e formalizando comandos principais do git

`git config`

`git remote`

`git pull <url>`

`git clone`

`git add .`

`git commit -m "msg"`

`git push`

`git revert`






